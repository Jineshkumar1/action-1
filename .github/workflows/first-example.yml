name: My First Workflow

on: push

jobs:
    first_job: 
        runs-on: ubuntu-latest
        steps:
            - name: Welcome Message
              run: echo "My First Github Action Job"

            - name: list files
              run: ls -lah

            - name: Read file
              run: cat README.md